---
title: "ライブラリとフレームワークの違い"
category: web-dev
tags: [architecture, language]
created: "2025-07-27"
updated: "2026-05-18"
freshness: stable
---

# 【比較】ライブラリとフレームワークの違い

ソフトウェア開発において、効率化のために利用される「ライブラリ」と「フレームワーク」。この2つは再利用可能なコードの集合体という点で共通していますが、開発における立ち位置と使い方、特に**プログラムの制御フロー（主導権）**において決定的な違いがあります。

## 概要

ライブラリとフレームワークを分ける本質は**制御の反転（Inversion of Control, IoC）**です。Martin Fowler の古典的な定義によれば、「ライブラリは呼び出せる関数の集合で、各呼び出しは仕事をして制御を呼び出し側に返す」のに対し、「フレームワークは抽象的な設計を内包し、フレームワーク側のコードがあなたのコードを（決められたポイントで）呼び出す」。つまり**ライブラリは『あなたが呼ぶ』、フレームワークは『あなたが呼ばれる（制御が反転する）』**もので、これを「ハリウッドの原則：我々を呼ぶな、我々が君を呼ぶ」と言い表します。

一言で言うと、**ライブラリは開発者が「使う」もの**であり、**フレームワークは開発者が「それに従って作る」もの**です。この主導権の違いが、両者を区別する最も重要なポイントです。

## 詳細比較

| 項目 | ライブラリ (Library) | フレームワーク (Framework) |
| --- | --- | --- |
| **役割** | 特定の機能を提供する**道具箱** | アプリケーションの**骨格・枠組み** |
| **制御の反転 (IoC)** | **開発者が**ライブラリの機能を呼び出す | **フレームワークが**開発者のコードを呼び出す |
| **主導権** | 開発者側にある | フレームワーク側にある |
| **使い方** | 必要な機能を必要な時に呼び出して使う | 決められた枠組みの中にコードを記述していく |
| **抽象度・自由度** | 低抽象・高自由（組み合わせ自由） | 高抽象・規約あり（流れはフレームワークが決める） |
| **例えるなら** | **工具セット** | **家の設計図**や**プラモデルの骨組み** |

### 1. ライブラリ (Library)

ライブラリは、特定の目的を持つ関数やクラスなどをまとめた、再利用可能なコードの集まりです。Martin Fowler の言葉では「呼び出せる関数の集合で、各呼び出しは仕事をして制御をクライアントに返す」もの。開発者は、アプリケーションを構築する上で必要となる様々な処理（例えば、日付のフォーマット、HTTPリクエストの送信、数値計算など）を、自ら実装する代わりにライブラリを呼び出すことで簡単に実現できます。

#### 主な特徴

- **開発者が主導**: アプリケーション全体の構造や処理の流れは開発者が設計します。ライブラリは、その中の一部分の処理を助ける「部品」として、開発者自身のコードから呼び出されます。
- **高い自由度**: どのライブラリを、いつ、どこで使うかは完全に開発者の自由です。複数のライブラリを自由に組み合わせることも容易です。
- **受動的な存在**: ライブラリは自ら動くことはなく、常に開発者からの呼び出しを待っています。
- **具体例**:
    - **jQuery**: DOM操作を簡単にするJavaScriptライブラリ。
    - **React**: UIを構築するためのJavaScriptライブラリ（公式も自らを「UI を構築するためのライブラリ」と位置づける。エコシステム込みでフレームワーク的に語られる側面もある）。
    - **Pandas (Python)**: データ分析を支援するライブラリ。
    - **Requests (Python)**: HTTP通信を簡単に行うためのライブラリ。

### 2. フレームワーク (Framework)

フレームワークは、アプリケーション開発における全体的な構造や設計の「枠組み」を提供します。単なる機能の集まりではなく、アプリケーションがどのように動作するかの大まかな流れ（骨格）が既に定義されています。開発者は、そのフレームワークが定めたルールや規約に従って、特定の部分に独自のコードを記述していくことでアプリケーションを完成させます。

#### 主な特徴

- **フレームワークが主導**: アプリケーションの起動からリクエストの受付、処理の実行、レスポンスの返却といった一連の流れはフレームワークが管理します。フレームワークは、特定のイベントが発生した際に、開発者が記述したコードを呼び出して実行します。この関係を**「制御の反転（Inversion of Control: IoC）」**と呼びます（Fowler「フレームワーク側のコードがあなたのコードを呼ぶ。制御が反転している」）。
- **規約と制約**: 「Don't Repeat Yourself (DRY)」の原則など、効率的で保守性の高い開発を促すための規約（CoC: Convention over Configuration）が定められていることが多く、開発者はそれに従う必要があります。
- **能動的な存在**: フレームワークはアプリケーションの土台として常に動いており、開発者のコードはその上で動きます。
- **具体例**:
    - **Ruby on Rails (Ruby)**: Webアプリケーションフレームワーク。
    - **Django (Python)**: Webアプリケーションフレームワーク。
    - **Angular (JavaScript)**: フロントエンド開発のフレームワーク。
    - **Spring (Java)**: Javaアプリケーション開発のための包括的なフレームワーク。

## よくある誤解

- **誤解1：「規模が大きい＝フレームワーク、小さい＝ライブラリ」** — 誤りです。区別の本質はサイズではなく**制御の所在（IoC）**です。Fowler が示すとおり、巨大でも「呼び出して使い、制御が返ってくる」ならライブラリ、小さくても「あなたのコードを呼ぶ（制御が反転する）」ならフレームワークの性質を持ちます。
- **誤解2：「React はフレームワーク」** — 半分誤りです。React 公式は自らを「UI を構築するためのライブラリ」と位置づけています。基本的には開発者が呼び出すライブラリで、ルーティングやデータ取得を含む完全な枠組みは Next.js 等のフレームワーク側が提供します（エコシステム全体を指して俗に「フレームワーク」と呼ばれることはある）。
- **誤解3：「フレームワークの方が常に優れている／高機能」** — 一面的です。フレームワークは規約と制御の主導権をフレームワークに渡す代わりに自由度を手放します。要件が限定的なら、必要なライブラリだけを自分の設計に組み込む方が軽量で柔軟なこともあります。
- **誤解4：「ライブラリとフレームワークは排他的」** — 誤りです。実際のアプリは「フレームワークの枠組みの中で、多数のライブラリを呼び出す」構成が普通です。両者は対立概念ではなく、制御の向きが違う再利用部品です。

## 実務での選び分け

- **特定機能だけが欲しい・既存設計に部品として足したい・自由度を最優先** → **ライブラリ**。組み合わせ自由で学習コストも局所的。
- **アプリ全体の構造を素早く立ち上げたい・規約に沿ってチームで大規模開発したい・定石に乗りたい** → **フレームワーク**。ルーティング・ライフサイクル・規約が用意され、車輪の再発明を避けられる。
- **段階的に始めたい** → ライブラリ中心で組み、共通化が増えてきたらフレームワーク採用を検討、という順も有効。
- **判断軸**：①制御をフレームワークに委ねてよいか（自由度 vs 定石） ②プロジェクト規模・寿命・チーム人数 ③規約学習コストと得られる生産性が見合うか。

「ハリウッドの原則」で言い換えると、ライブラリは開発者がハリウッド（ライブラリ）に電話して必要な俳優（機能）を呼び出して使う関係、フレームワークは開発者がオーディションに参加し、ハリウッド（フレームワーク）から役（実装すべき箇所）を与えられ呼び出されるのを待つ関係です。

## ひとことまとめ

ライブラリは「あなたが呼ぶ」道具箱、フレームワークは「あなたが呼ばれる」骨組み。両者を分けるのは規模ではなく制御の反転（IoC＝ハリウッドの原則）です。

## 出典・参考

- Martin Fowler「Inversion of Control」（ライブラリ＝呼び出せる関数群で各呼び出しが制御を呼び出し側に返す／フレームワーク＝抽象設計を内包しフレームワーク側があなたのコードを呼ぶ＝制御の反転。「Don't call us, we'll call you」）: https://martinfowler.com/bliki/InversionOfControl.html
- Wikipedia「Inversion of control」（手続き型ではユーザーコードがライブラリを呼ぶが、IoC では外部コード／フレームワークが制御を持ちユーザーコードを呼ぶ。ハリウッド原則「Don't call us, we'll call you」）: https://en.wikipedia.org/wiki/Inversion_of_control
- Wikipedia「Software framework」（フレームワークは拡張・カスタマイズして完成させる再利用可能な汎用機能で、低レベルコードに対する抽象層を提供する。IoC がフレームワークをライブラリと分ける鍵）: https://en.wikipedia.org/wiki/Software_framework
